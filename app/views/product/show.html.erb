<!-- banner -->
<%= render "banner" %>

<!-- breadcrumbs -->
<%= render partial: "breadcrumbs", locals: {title: @product.name} %>

<!-- single -->
<%= render partial: "single", locals: {product: @product} %>

