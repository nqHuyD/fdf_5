alert(`<%= @user.name %> has order <%= @total_product %> product (Order is: <%= @order.id %>)`)

$(".real_time_ajax_activity").html(`
  <% @activities.each do |activity| %>
    <%= render_notify activity %>
  <% end %>
  <%= paginate @activities, theme: "twitter-bootstrap-3" %>
`)

$(".container-bell").html(`
  <%= link_to "", admin_notify_data_path, class:"notification show-count notify",
        data:{count: activities_count} %>
`)
